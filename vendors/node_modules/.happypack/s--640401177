'use strict';

exports.__esModule = true;

var _extends2 = require('babel-runtime/helpers/extends');

var _extends3 = _interopRequireDefault(_extends2);

exports.changeMenuItem = changeMenuItem;

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

// Actions
var CHANGE_MENU_ITEM = 'yapi/menu/CHANGE_MENU_ITEM';

// Reducer
var initialState = {
  curKey: '/' + window.location.hash.split('/')[1]
};

exports.default = function () {
  var state = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : initialState;
  var action = arguments[1];

  if (action.type === CHANGE_MENU_ITEM) {
    return (0, _extends3.default)({}, state, {
      curKey: action.data
    });
  } else {
    return state;
  }
};

// Action Creators


function changeMenuItem(curKey) {
  return {
    type: CHANGE_MENU_ITEM,
    data: curKey
  };
}