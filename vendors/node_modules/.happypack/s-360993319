'use strict';

exports.__esModule = true;

var _extends2 = require('babel-runtime/helpers/extends');

var _extends3 = _interopRequireDefault(_extends2);

exports.getFollowList = getFollowList;
exports.addFollow = addFollow;
exports.delFollow = delFollow;

var _axios = require('axios');

var _axios2 = _interopRequireDefault(_axios);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

// Actions
var GET_FOLLOW_LIST = 'yapi/follow/GET_FOLLOW_LIST';
var DEL_FOLLOW = 'yapi/follow/DEL_FOLLOW';
var ADD_FOLLOW = 'yapi/follow/ADD_FOLLOW';

// Reducer
var initialState = {
  data: []
};

exports.default = function () {
  var state = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : initialState;
  var action = arguments[1];

  if (action.type === GET_FOLLOW_LIST) {
    return (0, _extends3.default)({}, state, {
      data: action.payload.data.data
    });
  } else {
    return state;
  }
};

// 获取关注列表


function getFollowList(uid) {
  return {
    type: GET_FOLLOW_LIST,
    payload: _axios2.default.get('/api/follow/list', {
      params: { uid: uid }
    })
  };
}

// 添加关注
function addFollow(param) {
  return {
    type: ADD_FOLLOW,
    payload: _axios2.default.post('/api/follow/add', param)
  };
}

// 删除关注
function delFollow(id) {
  return {
    type: DEL_FOLLOW,
    payload: _axios2.default.post('/api/follow/del', { projectid: id })
  };
}